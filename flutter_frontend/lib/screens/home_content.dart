import 'package:flutter/material.dart';

class HomeContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Vous pouvez personnaliser le contenu du corps ici en fonction de la navigation.
    return const Center(
      child: Text('HOME'),
    );
  }
}
